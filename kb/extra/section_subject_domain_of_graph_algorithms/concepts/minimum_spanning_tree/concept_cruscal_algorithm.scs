concept_cruscal_algorithm

<- sc_node_class;

=> nrel_main_idtf:
    [алгоритм Крускала]
    (* <- lang_ru;; *);
    [Cruscal algorithm]
    (* <- lang_en;; *);

=> nrel_note:
    [помогает найти минимальное связующее дерево для связанного взвешенного графа, добавляя дуги увеличивающейся стоимости на каждом шаге. Это алгоритм минимального связующего дерева, который находит ребро с наименьшим возможным весом, который соединяет любые два дерева в лесу.]
    (* <- lang_ru;; *);;